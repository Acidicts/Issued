<%= stylesheet_link_tag "pages/admin/users" %>

<h1>User Management</h1>
<div class="users-container">
    <table class="user-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Slack ID</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% @users.each do |user| %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.name %></td>
                    <td><%= user.slack_id.to_s.humanize %></td>
                    <td><%= user.role %></td>
                    <td class="action-buttons">
                        <%= link_to 'Edit', edit_admin_user_path(user), class: 'edit' %>
                        <%= link_to 'Delete', admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }, class: 'delete' %>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>

